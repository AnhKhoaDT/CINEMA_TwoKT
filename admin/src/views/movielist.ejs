<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Danh sách phim</title>
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <!-- Bootstrap Icons -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
        <link rel="stylesheet" href="./Movie/Movie.css">
        <link rel="stylesheet" href="sideBar.css">
    </head>    
    <body>
        <div class="layout-wrapper">
            <%- include('./components/sidebar') %> 

            <div class="movie-list flex-grow-1 p-4">
                <h2 class="mb-4">Danh sách phim</h2>
                
                <!-- Ô tìm kiếm tên phim -->
                <div class="mb-3 d-flex justify-content-between align-items-center">
                    <div class="input-group w-50">
                        <input type="text" class="form-control" id="search-movie" placeholder="Tìm kiếm tên phim...">
                        <button class="btn btn-primary" type="button" id="search-button">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </div>
                
                <table class="table table-bordered">
                    <thead>
                        <tr style="background-color: #f2f2f2;">
                            <th>Tên phim</th>
                            <th>Ngày phát hành</th>
                            <th>Thể loại</th>
                            <th>Ngày thêm phim</th>
                            <th>Hành động</th>
                        </tr>
                    </thead>
                    <tbody id="movie-list">
                        <!-- Nội dung động sẽ được thêm bằng JavaScript -->
                    </tbody>
                </table>
                
                <!-- Bộ chuyển trang -->
                <div class="pagination">
                    <button class="pagination-button" data-action="first"><i class="bi bi-caret-left-fill"></i></button>
                    <button class="pagination-button" data-action="prev"><i class="bi bi-caret-left"></i></button>
                    <span>Trang <span id="current-page">1</span> trên <span id="total-pages">1</span></span>
                    <button class="pagination-button" data-action="next"><i class="bi bi-caret-right"></i></button>
                    <button class="pagination-button" data-action="last"><i class="bi bi-caret-right-fill"></i></button>
                </div>
                
                
            </div>                    
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
        <script src="./Movie/MovieList.js"></script>
    </body>
</html>